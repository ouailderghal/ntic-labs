$TTL	604800
@	IN	SOA	s1.servers. root.s1.servers. (
			      2		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
; name server
@	IN	NS	s1.servers.

; servers
s1.servers.	    IN	A	10.0.0.1
vm.servers.	IN	A	10.0.0.3
